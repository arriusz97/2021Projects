using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FSMManager : FSM<FSMManager>
{
    // Start is called before the first frame update
    void Start()
    {
        //InitState(this, FSMManager.)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
